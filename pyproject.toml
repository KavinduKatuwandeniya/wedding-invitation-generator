[project]
name = "wedding-invitation-generator"
version = "1.0.0"
description = "Mobile-friendly wedding invitation PDF generator"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "python-multipart",
    "PyMuPDF",
    "openpyxl",
]
